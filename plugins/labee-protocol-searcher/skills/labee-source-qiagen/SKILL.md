---
name: labee-source-qiagen
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `qiagen` in the default source allowlist for Labee searches. Disable it to exclude QIAGEN from searches by default.
---

# QIAGEN source

Treat this skill's presence as configuration. Contribute `qiagen` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
