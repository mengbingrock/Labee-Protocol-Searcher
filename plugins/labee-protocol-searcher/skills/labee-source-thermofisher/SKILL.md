---
name: labee-source-thermofisher
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `thermofisher` in the default source allowlist for Labee searches. Disable it to exclude Thermo Fisher from searches by default.
---

# Thermo Fisher source

Treat this skill's presence as configuration. Contribute `thermofisher` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
