---
name: labee-source-nature-protocols
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `nature-protocols` in the default source allowlist for Labee searches. Disable it to exclude Nature Protocols from searches by default.
---

# Nature Protocols source

Treat this skill's presence as configuration. Contribute `nature-protocols` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
