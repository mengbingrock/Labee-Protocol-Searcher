---
name: labee-source-star-protocols
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `star-protocols` in the default source allowlist for Labee searches. Disable it to exclude STAR Protocols from searches by default.
---

# STAR Protocols source

Treat this skill's presence as configuration. Contribute `star-protocols` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
