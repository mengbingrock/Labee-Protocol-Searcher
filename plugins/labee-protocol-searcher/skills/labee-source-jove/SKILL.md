---
name: labee-source-jove
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `jove` in the default source allowlist for Labee searches. Disable it to exclude JoVE from searches by default.
---

# JoVE source

Treat this skill's presence as configuration. Contribute `jove` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
