---
name: labee-source-idt
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `idt` in the default source allowlist for Labee searches. Disable it to exclude IDT from searches by default.
---

# IDT source

Treat this skill's presence as configuration. Contribute `idt` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
