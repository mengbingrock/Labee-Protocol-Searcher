---
name: labee-source-rebase
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `rebase` in the default source allowlist for Labee searches. Disable it to exclude REBASE from searches by default.
---

# REBASE source

Treat this skill's presence as configuration. Contribute `rebase` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
