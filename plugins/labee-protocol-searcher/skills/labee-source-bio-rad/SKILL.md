---
name: labee-source-bio-rad
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `bio-rad` in the default source allowlist for Labee searches. Disable it to exclude Bio-Rad from searches by default.
---

# Bio-Rad source

Treat this skill's presence as configuration. Contribute `bio-rad` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
