---
name: labee-source-neb
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `neb` in the default source allowlist for Labee searches. Disable it to exclude NEB from searches by default.
---

# NEB source

Treat this skill's presence as configuration. Contribute `neb` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
