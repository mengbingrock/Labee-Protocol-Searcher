---
name: labee-source-promega
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `promega` in the default source allowlist for Labee searches. Disable it to exclude Promega from searches by default.
---

# Promega source

Treat this skill's presence as configuration. Contribute `promega` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
