---
name: labee-source-bio-protocol
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `bio-protocol` in the default source allowlist for Labee searches. Disable it to exclude Bio-protocol from searches by default.
---

# Bio-protocol source

Treat this skill's presence as configuration. Contribute `bio-protocol` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
