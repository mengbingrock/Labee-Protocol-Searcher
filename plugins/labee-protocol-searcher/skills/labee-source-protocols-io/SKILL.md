---
name: labee-source-protocols-io
description: Publisher-selection marker for Labee Protocol Searcher. When this skill is enabled in plugin settings, include source id `protocols-io` in the default source allowlist for Labee searches. Disable it to exclude protocols.io from searches by default.
---

# protocols.io source

Treat this skill's presence as configuration. Contribute `protocols-io` to the `sources` array passed to Labee's `search` tool. Let the main Labee Protocol Searcher skill coordinate the search.
